---
description: Show installed invincible version vs this project's recorded version and whether an update is needed.
---

Run the following and report the output:

```bash
invincible status --root .
```

If the output shows "update available", tell the user to run `/invincible-update` to refresh the project's configuration files.
