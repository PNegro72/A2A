---
description: Refresh this project's invincible configuration files to match the currently installed invincible version.
---

Run:

```bash
invincible update --root .
```

Then confirm the update succeeded:

```bash
invincible status --root .
```

Report any files that were updated, and alert the user if errors occurred.
