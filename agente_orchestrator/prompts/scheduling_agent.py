# Deprecated — no longer used.
# Scheduling agent instructions now live in the agent card (registry/fallback_cards/scheduling_agent.json).
# Orchestrator instruction is built in orchestrator/prompts/orchestrator.py.
